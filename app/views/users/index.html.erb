<h1 class="text-center mt-4">Alpha bloggers</h1>

<div class="container">
  <% @users.each do |user|%>
    <div class="row justify-content-md-center ">
      <div class="col-8 mt-4">
        <div class="card text-center shadow mb-5 bg-white rounded">
          <div class="card-header font-italic">
             <%= link_to user.username, user %>
          </div>
          <div class="card-body ">
            <h5 class="card-title">
            <%= link_to gravatar_for(user, size: 150), user %>
            </h5>
            <p class="card-text"><%= pluralize(user.articles.count, "article") %></p>
            <div id="btn-container-users">
            <%= link_to "View profile", user, class: "btn btn-outline-success m-1" %></a>
            <% if logged_in? %>
              <% if user == current_user %>
              <%= link_to "Edit pofile", edit_user_path(user), method: :edit, class: "btn btn-outline-info m-1" %>
              <% end %> 
            <% if current_user.admin? %>
            <%= button_to "Delete user", user_path(user), class: "btn btn-outline-danger m-1", method: :delete, onclick: "return confirm('Are you sure you want to delete the user account and all articles associated?')" %>
            <% end %>

            <% end %>
           </div>
          </div>
          <div class="card-footer text-muted">
            <small>Joined <%= time_ago_in_words(user.created_at) %> ago</small>
          </div>
        </div>
       </div>
      </div>
  <% end %>
</div>